{
    "name": "szepeviktor/toolkit4wp",
    "description": "OOP toolkit for daily tasks in WordPress development.",
    "keywords": [
        "tools",
        "oop",
        "wordpress"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1"
    },
    "require-dev": {
        "szepeviktor/phpcs-psr-12-neutron-hybrid-ruleset": "^0.4",
        "dealerdirect/phpcodesniffer-composer-installer": "^0.7",
        "szepeviktor/phpstan-wordpress": "^0.7"
    },
    "suggest": {
    },
    "config": {
        "optimize-autoloader": true
    },
    "autoload": {
        "psr-4": {
            "Toolkit4WP\\": "src/"
        },
        "files": [
            "src/helpers.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Toolkit4WP\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "syntax": "find -L . -path ./vendor -prune -o -name '*.php' -print0 | xargs -0 -n 1 -P 4 -- php -l",
        "style": "phpcs -p --parallel=4 --standard=PSR12NeutronRuleset src/",
        "phpstan": "phpstan analyze",
        "test": [
            "@composer validate --strict",
            "@syntax",
            "@style",
            "@phpstan"
        ]
    }
}
